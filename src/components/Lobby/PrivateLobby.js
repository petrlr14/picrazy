import React, { useEffect } from "react";
import { useRouteMatch } from "react-router-dom";

export const PrivateLobby = ({ roomId }) => {
  /* useEffect(() => {
    const roomId = useRouteMatch().params["roomId"];
    if(roomI)
  }); */
  return <>Private lobby</>;
};
